#include <stdio.h>

int main(void) {

    // int a[4];
    // printf("%d\n", a[0]);
    // printf("%d", sizeof(a)/sizeof(int));
    // return 0;

    int a[4];
    printf("%d\n", a[0]);
    printf("%d", sizeof(a)/sizeof(int));
    return 0;
}