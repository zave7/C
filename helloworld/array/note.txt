배열

1. 기초
    1) 배열도 선언할때는 반드시 초기화를 해줘야한다.
    2) 초기화하지 않으면 쓰레기값으로 채워진다.
    3) 배열은 보통 0으로 초기화 한다 ex) {0}, {0,}
        - 배열에서 첫번째 원소의 초기값만 설정하면 나머지 원소는 컴파일러가 자동으로 0 으로 설정함
    4) 원소의 개수를 생략할 수도 있다 ( 선언과 동시 할당)